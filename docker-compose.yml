version: '3.8'

services:
  app:
    build: .
    container_name: conductorai_app
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:rw
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
